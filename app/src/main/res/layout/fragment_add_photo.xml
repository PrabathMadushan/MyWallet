<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="400dp"
    tools:context=".fregments.AddPhotoFragment">

<androidx.constraintlayout.widget.ConstraintLayout
    android:id="@+id/fwaraper"
    android:layout_width="match_parent"
    android:layout_height="match_parent">
    <ImageView
        android:id="@+id/photoDocument"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:scaleType="fitXY"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:background="@drawable/recordimage"
        />

    <ImageButton
        android:id="@+id/btnOpenPhoto"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_marginTop="8dp"
        android:background="@drawable/style_button_circle_rose"
        android:tint="@android:color/white"
        app:layout_constraintEnd_toEndOf="@+id/btnOpenCamara"
        app:layout_constraintStart_toStartOf="@+id/btnOpenCamara"
        app:layout_constraintTop_toBottomOf="@+id/btnOpenCamara"
        app:srcCompat="@drawable/ic_nui_menu_gallery" />

    <ImageButton
        android:id="@+id/btnOpenCamara"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_marginStart="8dp"
        android:layout_marginTop="60dp"
        android:background="@drawable/style_button_circle_rose"
        android:tint="@android:color/white"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:srcCompat="@drawable/ic_nui_menu_camera" />


</androidx.constraintlayout.widget.ConstraintLayout>

</androidx.constraintlayout.widget.ConstraintLayout>